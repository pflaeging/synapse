apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  annotations:
    deployment.kubernetes.io/revision: "1"
  creationTimestamp: null
  generation: 1
  labels:
    net.pflaeging.deployment: chat-stickiebox-postgresql
  name: chat-stickiebox-postgresql
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 10
  selector:
    matchLabels:
      net.pflaeging.deployment: chat-stickiebox-postgresql
  strategy:
    type: Recreate
  template:
    metadata:
      creationTimestamp: null
      labels:
        net.pflaeging.deployment: chat-stickiebox-postgresql
    spec:
      containers:
      - envFrom:
        - configMapRef:
            name: chat-stickiebox
        image: docker-registry.default.svc:5000/sb/postgresql:latest
        imagePullPolicy: Always
        name: idp-pgsql
        resources: {}
        terminationMessagePath: /dev/termination-log
        terminationMessagePolicy: File
        volumeMounts:
        - mountPath: /var/lib/pgsql/data
          name: chat-stickiebox-pg-data
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30
      volumes:
      - name: chat-stickiebox-pg-data
        persistentVolumeClaim:
          claimName: chat-stickiebox-pg-data
status: {}